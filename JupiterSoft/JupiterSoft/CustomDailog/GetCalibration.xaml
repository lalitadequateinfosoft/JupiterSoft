<Window x:Class="JupiterSoft.CustomDailog.GetCalibration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JupiterSoft.CustomDailog"
        mc:Ignorable="d"
        Title="Calibration Configuration" 
        WindowStyle="None"
        AllowsTransparency="True"
        Height="650"
        Width="800"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Grid Background="Transparent">
        <Border 
                CornerRadius="5"
                BorderThickness="1"
                BorderBrush="#0d447a"
                Background="White"
                Margin="10"
                >
            <Border.Effect>
                <DropShadowEffect Color="Black"
                          Direction="270"
                          BlurRadius="12"
                          ShadowDepth="3" />
            </Border.Effect>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Border 
                            Grid.Row="0"
                            Margin="0.8"
                            Background="#0d447a"
                            BorderBrush="#0d447a"
                            BorderThickness="1"
                            CornerRadius="5,5,0,0"
                            Padding="15">
                    <Label x:Name="HeaderTitle" Content="Set Calibration Factors" Foreground="White"/>
                </Border>
                <ScrollViewer Grid.Row="1" 
                            x:Name="OutPutArea"
                            VerticalScrollBarVisibility="Auto">

                    <Grid 
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Button x:Name="AddCalibration" HorizontalAlignment="Right" Content="Add" Click="AddCalibration_Click" Margin="5" Background="ForestGreen" Foreground="White"/>
                        </Grid>

                        <Grid
                                    Grid.Row="1"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="5,5,5,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Content="STEP"
                                       Grid.Column="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"
                                       HorizontalContentAlignment="Center" BorderBrush="#686868" BorderThickness="1"/>
                                <Label Content="OPERATION"
                                       Grid.Column="1"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Center"
                                       VerticalAlignment="Stretch" BorderBrush="#686868" BorderThickness="1"/>
                                <Label Content="FACTOR"
                                       Grid.Column="2"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Center"
                                       VerticalAlignment="Stretch" BorderBrush="#686868" BorderThickness="1"/>
                                <Label Content="ACTION"
                                       Grid.Column="3"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Center"
                                       VerticalAlignment="Stretch" BorderBrush="#686868" BorderThickness="1"/>
                            </Grid>
                            <ListView Grid.Row="1"
                                    x:Name="logsdata"
                                    ItemsSource="{Binding Logs}"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"
                                      Padding="0"
                                      Margin="0"
                                      SelectionMode="Extended">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="Focusable" Value="False"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="192"/>
                                                <ColumnDefinition Width="192"/>
                                                <ColumnDefinition Width="192"/>
                                                <ColumnDefinition Width="192"/>
                                            </Grid.ColumnDefinitions>
                                           
                                                <Label Content="{Binding Path=OutPutCom}"
                                                       Grid.Column="0"
                                                       BorderBrush="#686868"
                                                       BorderThickness="1" 
                                                       HorizontalAlignment="Stretch" 
                                                       VerticalAlignment="Stretch"
                                                       HorizontalContentAlignment="Center"/>
                                           
                                           
                                                <ComboBox SelectedValuePath="Content"
                                                          Tag="{Binding Path=id}"
                                                          SelectionChanged="ComboBox_SelectionChanged"
                                                          Grid.Column="1"
                                                          BorderBrush="#686868"
                                                          BorderThickness="1"
                                                          HorizontalAlignment="Stretch"
                                                          VerticalAlignment="Stretch"
                                                          HorizontalContentAlignment="Center"
                                                          SelectedValue="{Binding Path=CommandText}">
                                                    <ComboBoxItem Content="Add"/>
                                                    <ComboBoxItem Content="Subtract"/>
                                                    <ComboBoxItem Content="Multiply"/>
                                                    <ComboBoxItem Content="Divide"/>
                                                </ComboBox>
                                         
                                            
                                                <TextBox Tag="{Binding Path=id}"
                                                         TextChanged="TextBox_TextChanged"
                                                         Text="{Binding Path=mVal,Mode=TwoWay}"
                                                          Grid.Column="2"
                                                         BorderBrush="#686868"
                                                         BorderThickness="1"
                                                         HorizontalAlignment="Stretch"
                                                         VerticalAlignment="Stretch"
                                                         HorizontalContentAlignment="Center"/>
                                            
                                           
                                                <Button  Click="Remove_Click"
                                                         Content="REMOVE"
                                                         Tag="{Binding Path=id}"
                                                         Grid.Column="3"
                                                         BorderBrush="#686868"
                                                         BorderThickness="1"
                                                         HorizontalAlignment="Stretch"
                                                         VerticalAlignment="Stretch"
                                                         HorizontalContentAlignment="Center"/>
                                            
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Grid>
                </ScrollViewer>
                <Grid 
                    Grid.Row="2"
                    Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Button 
                            Background="#0d447a"
                            BorderBrush="#0d447a"
                            Foreground="White"
                            Content="Submit"
                        HorizontalAlignment="Right"
                        Margin="0,5,5,0"
                        Click="BtnOk_Click"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <Button 
                            Background="Red"
                            BorderBrush="Red"
                            Foreground="White"
                            Content="Cancel"
                            HorizontalAlignment="Left"
                            Margin="5,5,0,0"
                            Click="BtnCancel_Click"/>
                    </StackPanel>



                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
